# Claude Code instructions

## Git & GitHub

- Never include Claude session URLs, Claude branding, or any reference to Claude in commit messages, PR titles, PR descriptions, code comments, or any other repository artifact.
- Never use `Claude` as the git author or committer name, `noreply@anthropic.com` as the email, or add any `Co-authored-by: Claude` trailers. Commits must contain no Claude identity anywhere.
